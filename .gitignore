/target/
**/*.rs.bk
*.pdb
.idea/*
!.idea/runConfigurations/
.vscode/
.claude/
.env
*.log
# Local project handoff notes for Claude sessions (not committed by request).
/CLAUDE.md
/target/pub/

# siege demo: raw model-kit downloads. We extract the few .glb we actually use
# and commit those; the bulky zips and the dedup scratch folder stay local.
crates/vectorial-hash-demos/assets/siege/*.zip
crates/vectorial-hash-demos/assets/siege/quitar o repes/
crates/vectorial-hash-demos/assets/siege/Fish.zip
# Loose root-level .glb downloads — the ones we use are extracted into models/.
crates/vectorial-hash-demos/assets/siege/*.glb
bench-results/
vh-calibration.txt
